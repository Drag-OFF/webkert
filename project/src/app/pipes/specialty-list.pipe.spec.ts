import { SpecialtyListPipe } from './specialty-list.pipe';

describe('SpecialtyListPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialtyListPipe();
    expect(pipe).toBeTruthy();
  });
});
