import { ArtistNameUpperPipe } from './artist-name-upper.pipe';

describe('ArtistNameUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistNameUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
